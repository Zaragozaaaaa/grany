- AI-ключ Anthropic хранится только в секретах Supabase Edge Function, никогда не передаётся и не логируется на фронте
- Все вызовы Anthropic API происходят только из edge-функций, не из браузера
- Каждая запись fact_entry и user_skill обязательно привязана к user_id
- AI-функции извлечения (analyze, skill_ladder) возвращают строго валидный JSON без markdown-обёрток (```)
- Грани CASEL берутся только из справочника dim_node, выдуманные значения недопустимы
- .env и любые файлы с секретами добавлены в .gitignore и не коммитятся
- Ответ edge-функции mirror укладывается в ~10 секунд
